# ระบบแจ้งเตือน (Notification System) - ResCash

## 📋 ภาพรวมระบบ

ระบบแจ้งเตือนของ ResCash ถูกออกแบบมาให้ส่งการแจ้งเตือนอัตโนมัติไปยังผู้ใช้ที่เกี่ยวข้องตาม `user.role` และ `order.order_status`

## 🎯 หลักการทำงาน

ระบบจะส่งการแจ้งเตือนไปยัง:
- **ลูกค้า (Customer)**: ผู้สั่งอาหารจะได้รับการอัพเดทเกี่ยวกับออร์เดอร์ของตัวเอง
- **พนักงาน (Staff)**: จะได้รับแจ้งเตือนเกี่ยวกับออร์เดอร์ใหม่และสถานะที่ต้องดำเนินการ
- **ผู้ดูแลระบบ (Admin)**: จะได้รับแจ้งเตือนเกี่ยวกับสลิปที่ต้องตรวจสอบและเหตุการณ์สำคัญ

## 📊 Database Schema

```sql
CREATE TABLE IF NOT EXISTS notification (
    noti_id SERIAL PRIMARY KEY,
    user_id INT REFERENCES Users(user_id),      -- ผู้รับการแจ้งเตือน
    order_id INT REFERENCES "Order"(order_id),  -- ออร์เดอร์ที่เกี่ยวข้อง
    message TEXT,                                -- ข้อความแจ้งเตือน
    create_datetime TIMESTAMP DEFAULT now(),     -- เวลาที่สร้าง
    is_read BOOLEAN DEFAULT FALSE               -- สถานะการอ่าน
);
```

## 🔄 การแจ้งเตือนตาม Order Status

### 1. `awaiting_payment` - รอชำระเงิน
**เมื่อไหร่**: สร้าง order ใหม่  
**แจ้งเตือนใคร**: ลูกค้าที่สั่ง  
**ข้อความตัวอย่าง**: 
- Customer: `⏳ รอชำระเงิน - ออร์เดอร์ ORD-20251028-001 จำนวน 350.00 บาท กรุณาชำระเงินก่อน 12 ชม. ของเวลารับอาหาร`

---

### 2. `awaiting_admin_review` - รอตรวจสอบสลิป
**เมื่อไหร่**: ลูกค้าอัพโหลดสลิปการโอนเงิน  
**แจ้งเตือนใคร**: 
- ลูกค้าที่สั่ง (ยืนยันว่าได้รับสลิปแล้ว)
- Admin ทุกคน (ให้ตรวจสอบสลิป)

**ข้อความตัวอย่าง**: 
- Customer: `🔍 รอตรวจสอบสลิป - ออร์เดอร์ ORD-20251028-001 ทางเรากำลังตรวจสอบการชำระเงินของคุณ`
- Admin: `💰 สลิปใหม่รอตรวจสอบ - ORD-20251028-001 ลูกค้า: สมชาย ใจดี จำนวน: 350.00 บาท`

---

### 3. `order_recived` - ร้านรับออร์เดอร์แล้ว
**เมื่อไหร่**: Admin อนุมัติสลิปและเปลี่ยนสถานะ  
**แจ้งเตือนใคร**: 
- ลูกค้าที่สั่ง
- Staff ทุกคน (ให้เริ่มเตรียมอาหาร)
- Admin ทุกคน (เพื่อติดตาม)

**ข้อความตัวอย่าง**: 
- Customer: `✅ ร้านรับออร์เดอร์แล้ว - ออร์เดอร์ ORD-20251028-001 ทางร้านกำลังเตรียมอาหารของคุณ`
- Staff: `📋 ออร์เดอร์ใหม่! - ORD-20251028-001 ลูกค้า: สมชาย ใจดี รับเวลา: 28 ต.ค. 2568 14:00`
- Admin: `✅ ยืนยันสลิปแล้ว - ORD-20251028-001 ลูกค้า: สมชาย ใจดี`

---

### 4. `preparing` - กำลังเตรียมอาหาร
**เมื่อไหร่**: Staff เริ่มทำอาหาร  
**แจ้งเตือนใคร**: 
- ลูกค้าที่สั่ง
- Staff ทุกคน (เพื่อให้รู้ว่า order ไหนกำลังทำ)

**ข้อความตัวอย่าง**: 
- Customer: `👨‍🍳 กำลังเตรียมอาหาร - ออร์เดอร์ ORD-20251028-001 กำลังทำอาหารของคุณ`
- Staff: `👨‍🍳 เริ่มเตรียมอาหาร - ORD-20251028-001 ลูกค้า: สมชาย ใจดี`

---

### 5. `ready_for_pickup` - อาหารพร้อมรับ ⭐ สำคัญมาก
**เมื่อไหร่**: อาหารเสร็จแล้วพร้อมส่งมอบ  
**แจ้งเตือนใคร**: 
- ลูกค้าที่สั่ง (แจ้งให้มารับ)
- Staff ทุกคน (พร้อมส่งมอบ)

**ข้อความตัวอย่าง**: 
- Customer: `🔔 อาหารพร้อมรับแล้ว! - ออร์เดอร์ ORD-20251028-001 สามารถมารับได้ที่ร้าน เวลา 28 ต.ค. 2568 14:00`
- Staff: `✅ อาหารพร้อมส่ง - ORD-20251028-001 ลูกค้า: สมชาย ใจดี โทร: 081-234-5678`

---

### 6. `completed` - คำสั่งซื้อเสร็จสมบูรณ์
**เมื่อไหร่**: ส่งมอบอาหารให้ลูกค้าแล้ว  
**แจ้งเตือนใคร**: 
- ลูกค้าที่สั่ง (ขอบคุณ)
- Staff ทุกคน (บันทึกว่าส่งมอบแล้ว)

**ข้อความตัวอย่าง**: 
- Customer: `✨ คำสั่งซื้อเสร็จสมบูรณ์ - ออร์เดอร์ ORD-20251028-001 ขอบคุณที่ใช้บริการ ResCash`
- Staff: `✨ ส่งมอบสำเร็จ - ORD-20251028-001 ลูกค้า: สมชาย ใจดี`

---

### 7. `cancelled` - ออร์เดอร์ถูกยกเลิก
**เมื่อไหร่**: ลูกค้าหรือ Staff/Admin ยกเลิก order  
**แจ้งเตือนใคร**: 
- ลูกค้าที่สั่ง
- Staff ทุกคน
- Admin ทุกคน

**ข้อความตัวอย่าง**: 
- Customer: `❌ ออร์เดอร์ถูกยกเลิก - ออร์เดอร์ ORD-20251028-001 ถูกยกเลิกแล้ว`
- Staff: `❌ ยกเลิกออร์เดอร์ - ORD-20251028-001 ลูกค้า: สมชาย ใจดี`
- Admin: `❌ ยกเลิกออร์เดอร์ - ORD-20251028-001 ลูกค้า: สมชาย ใจดี จำนวน: 350.00 บาท`

---

### 8. `payment_timeout` - หมดเวลาชำระเงิน
**เมื่อไหร่**: ไม่ชำระเงินภายใน 12 ชั่วโมงก่อนเวลารับ (Cron Job)  
**แจ้งเตือนใคร**: 
- ลูกค้าที่สั่ง
- Admin ทุกคน

**ข้อความตัวอย่าง**: 
- Customer: `⏰ ออร์เดอร์หมดเวลา - ออร์เดอร์ ORD-20251028-001 หมดเวลาชำระเงินแล้ว`
- Admin: `⏰ ออร์เดอร์หมดเวลา - ORD-20251028-001 ลูกค้า: สมชาย ใจดี`

---

## 💻 วิธีใช้งาน

### ฟังก์ชันหลัก: `notifyOrderStatusChange`

```typescript
import { notifyOrderStatusChange } from "@/lib/utils/notificationUtils";
import { createClient } from "@/lib/supabase/client";

const supabase = createClient();
const orderId = 123;
const newStatus = "ready_for_pickup";

// ส่ง notification ให้ทุกคนที่เกี่ยวข้องตาม status
const result = await notifyOrderStatusChange(supabase, orderId, newStatus);

console.log(result);
// {
//   success: true,
//   results: [
//     { role: "customer", userId: 5, success: true, ... },
//     { role: "staff", userId: 10, success: true, ... },
//     { role: "staff", userId: 11, success: true, ... }
//   ]
// }
```

### ตัวอย่างการใช้งานในแต่ละจุด

#### 1. สร้าง Order ใหม่
```typescript
// ใน createOrder.ts
const orderId = await createOrder(orderData);
await notifyOrderStatusChange(supabase, orderId, "awaiting_payment");
```

#### 2. อัพโหลดสลิปการโอนเงิน
```typescript
// ใน payment page
await supabase.from("order").update({ 
  slip_url: slipUrl,
  order_status: "awaiting_admin_review" 
});
await notifyOrderStatusChange(supabase, orderId, "awaiting_admin_review");
```

#### 3. เปลี่ยนสถานะ Order
```typescript
// ใน order detail page
await supabase.from("order").update({ order_status: nextStatus });
await notifyOrderStatusChange(supabase, orderId, nextStatus);
```

#### 4. ยกเลิก Order
```typescript
await supabase.from("order").update({ order_status: "cancelled" });
await notifyOrderStatusChange(supabase, orderId, "cancelled");
```

#### 5. Cron Job - Payment Timeout
```typescript
// ใน checkPaymentTimeouts.ts
await supabase.from("order").update({ order_status: "payment_timeout" });
await notifyOrderStatusChange(supabase, orderId, "payment_timeout");
```

---

## 🔍 ข้อมูลที่แสดงในข้อความ

การแจ้งเตือนแต่ละครั้งจะดึงข้อมูลจาก database มาแสดง:

1. **หมายเลขออร์เดอร์** (`public_order_id`): ORD-20251028-001
2. **ชื่อลูกค้า** (`user.name`): สมชาย ใจดี
3. **เบอร์โทรลูกค้า** (`user.phone`): 081-234-5678
4. **จำนวนเงิน** (`total_amount`): 350.00 บาท
5. **เวลานัดรับ** (`appointment_time`): 28 ต.ค. 2568 14:00
6. **เวลาสร้าง notification** (`create_datetime`): อัตโนมัติ

---

## 📱 การแสดงผล

การแจ้งเตือนจะแสดงใน:
- **Notification Page** (`/notification`): หน้าแสดงรายการแจ้งเตือนทั้งหมด
- **Notification Icon**: แสดงจำนวนการแจ้งเตือนที่ยังไม่ได้อ่าน
- **Real-time Updates**: ใช้ Supabase Realtime เพื่ออัพเดทแบบ real-time

---

## ✅ หลักการออกแบบ

1. **ไม่ใช้ console.error**: ใช้ `console.log` เท่านั้น
2. **Insert เข้า Supabase**: ทุก notification จะถูกบันทึกใน database
3. **เหมาะสมกับ Role**: แต่ละ role จะได้รับข้อความที่เหมาะสม
4. **แสดงข้อมูลครบถ้วน**: ชื่อ, เวลา, หมายเลข order
5. **ตาม ER Diagram**: ตรงตาม schema ของ database
6. **ทำงานได้จริง**: ทุก function ถูกทดสอบและเชื่อมต่อกับระบบ

---

## 🔧 Backward Compatibility

ระบบยังรองรับฟังก์ชันเก่าเพื่อไม่ให้ code เดิมพัง:

```typescript
// ⚠️ Deprecated - ใช้ได้แต่แนะนำให้ใช้ฟังก์ชันใหม่
createOrderNotificationClient(supabase, orderId, userId, trigger)
notifyAdminsNewSlip(supabase, orderId)
notifyStaffOrderReceived(supabase, orderId)
```

---

## 📝 สรุป

ระบบแจ้งเตือนของ ResCash ออกแบบมาให้:
- ✅ ครบถ้วนทุก order status
- ✅ เหมาะสมกับทุก user role
- ✅ แสดงข้อมูลที่สำคัญครบถ้วน
- ✅ ใช้งานง่าย เพียง 1 function
- ✅ ทำงานได้จริงกับ Supabase
- ✅ ตรงตาม ER Diagram

---

**Created by**: GitHub Copilot  
**Date**: October 28, 2025  
**Version**: 2.0
